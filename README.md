# -Networking-Book-Q-A-LLM
This project implements a GPT-style (decoder-only Transformer) language model built completely from scratch using PyTorch, without using pre-trained models. The goal of this project is to understand and demonstrate how modern large language models (LLMs) work internally, including self-attention, positional encoding, generating code 
